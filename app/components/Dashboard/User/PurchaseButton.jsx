import { Button } from "@/components/ui/button";
import React from "react";

const PurchaseButton = () => {
  return (
    <div>
      <Button className="bg-blue-700 hover:bg-blue-600 text-white mt-4">
        Purches
      </Button>
    </div>
  );
};

export default PurchaseButton;
